
import React from 'react';

const PageTitle = ({title}) => {
    return (
        <div className=" w-full">
            <h1 className="font-bold font-sans text-blue-500 text-4xl">{title}</h1>
        </div>
    )
}

export default PageTitle;